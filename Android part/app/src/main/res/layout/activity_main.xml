<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="info.sayederfanarefin.myapplication.MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Thesis"
        android:id="@+id/text_title"
        android:padding="12dp"
        android:textSize="24dp"
        android:textAlignment="center"/>


    <Button
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:text="Stored locations List"
        android:onClick="selfDestruct"
        android:layout_below="@+id/text_title"
        android:id="@+id/button_locations_list"/>

    <Button
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:text="Stored locations map"
        android:onClick="selfDestruct"
        android:layout_below="@+id/button_locations_list"
        android:id="@+id/button_locations_map"
        />

    <!--<Button-->
        <!--android:layout_height="wrap_content"-->
        <!--android:layout_width="match_parent"-->
        <!--android:text="Stored locations map"-->
        <!--android:onClick="selfDestruct"-->
        <!--android:layout_below="@+id/button_locations_map"-->
        <!--android:id="@+id/button_current_location"/>-->

</RelativeLayout>
